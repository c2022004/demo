package com.example.projectsale.userprofile.repo;

import com.example.projectsale.userprofile.entity.UserProfile;
import com.example.projectsale.utils.AbstractRepository;

import java.util.UUID;

public interface UserProfileRepo extends AbstractRepository<UserProfile, UUID> {
}
